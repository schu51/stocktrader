name: Intraday Exit Monitor

on:
  schedule:
    # :15 and :45 past each hour, 9 AM–4 PM EDT (13–20 UTC)
    # Offset from portfolio sync (:00 and :30) to avoid git push conflicts
    - cron: '15,45 13-20 * * 1-5'
  workflow_dispatch:

jobs:
  exit-monitor:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    permissions:
      contents: write
    env:
      ALPACA_API_KEY: ${{ secrets.ALPACA_API_KEY }}
      ALPACA_SECRET_KEY: ${{ secrets.ALPACA_SECRET_KEY }}
      ALPACA_PAPER: ${{ secrets.ALPACA_PAPER }}

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v4
        with:
          python-version: '3.12'
          cache: pip

      - run: pip install -r requirements.txt

      - name: Run intraday exit monitor
        run: python intraday_exit.py

      - name: Commit result
        if: always()
        run: |
          git config user.email "aschumacherdesign@gmail.com"
          git config user.name "schu51"
          git add docs/data/intraday_exit.json docs/data/trades.json docs/data/sma50_cache.json 2>/dev/null || true
          git diff --staged --quiet || git commit -m "Intraday exit check [skip ci]"
          pushed=0
          for attempt in 1 2 3 4 5; do
            if git pull --rebase -X theirs origin main && git push; then pushed=1; break; fi
            echo "push attempt $attempt failed, retrying..."
            git rebase --abort 2>/dev/null || true
            sleep $((RANDOM % 4 + 2))
          done
          [ "$pushed" = "1" ] || { echo "::error::push failed after 5 attempts"; exit 1; }
